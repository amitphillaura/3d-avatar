// router.js — client-side view router stub.
// TODO: implement SPA routing if multiple top-level views are needed.
// Currently the app is single-page; this file is reserved for future use.

export function initRouter() {
  // No-op stub — extend with hash-based or History API routing as needed.
}
