# Face models

Drop face or head **GLB** files here for the Face row in the app.

Register each file in `../registry.json` under the `face` array.

Face pose retargeting is not wired yet — slots show a static preview once the file is present.
